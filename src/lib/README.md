# Lib

Utilities and shared configuration:

- **utils.ts** – General helpers (e.g. `cn` for class names)
- **api.ts** – API client and base URL for backend calls
- **theme.ts** – Material UI theme (primary blue, secondary red, typography, component defaults)
