# Hooks

Custom React hooks for data and UI:

- **useRoutes.ts** – Fetches routes list (React Query)
- **useTasks.ts** – Fetches tasks list (React Query)

Add more hooks here for alerts, analytics, or local state.
