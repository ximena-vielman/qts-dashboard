# UI components

Reusable UI components and Shadcn/ui primitives. Use the barrel export from `@/components/ui` or import individual components (e.g. `@/components/ui/button`). Includes Button, Card, Badge, Avatar, Separator, Skeleton, Toast, Tooltip, and related Shadcn components.
