# Layout components

Layout components for the QTS Dashboard:

- **DashboardLayout** – Top bar (QTS branding) + left sidebar + main content. Uses MUI AppBar and Drawer; responsive.
- **Sidebar** – Left navigation (Dashboard, Routes, Tasks, Alerts, Analytics).

Used by the root page and the (routes) group layout.
